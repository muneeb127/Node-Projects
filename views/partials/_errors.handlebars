{{!-- Passport.js sends only one error --}}
{{#if error}}
    <div class="alert alert-danger">{{error}}</div>
{{/if}}


{{!-- This is the error array we are sending --}}
{{!-- loop thorugh errors array in handlebars--}}
{{#each errors}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}
{{!-- Nothing --}}
{{/each}}